mkdir -p "$LIBV/conf/serialbattery"
if test ! -s "$LIBV/conf/serialbattery/default.ini" ; then
    cp -a "$HERE/ext/dbus-serialbattery/dbus-serialbattery/config.default.ini" "$LIBV/conf/serialbattery/default.ini"
fi

